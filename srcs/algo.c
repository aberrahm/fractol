# include "../includes/fractol.h"

void    ft_resize(t_all img)
{
    
}


int     ft_julia(t_all *point)
{
    int     z;
    int     c;


    z = z * z + c;
    while (z < 4)
    {

        point->ite++;
    }
    return (point->ite);
}
